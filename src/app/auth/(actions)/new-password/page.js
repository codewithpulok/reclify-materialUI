import { NewPasswordView } from 'src/sections/auth';

export const metadata = {
  title: 'New Password - Racklify',
};

const NewPasswordPage = (props) => <NewPasswordView />;

NewPasswordPage.propTypes = {};

export default NewPasswordPage;
