import UserSettingsView from 'src/sections/private/settings/view';

export const metadata = {
  title: 'User Settings',
};

const UserSettingsPage = () => <UserSettingsView />;

export default UserSettingsPage;
