import MediaView from 'src/sections/public/marketing/media';
// ----------------------------------------------------------------------

export const metadata = {
  title: 'Media',
};

const MediaPage = () => <MediaView />;

export default MediaPage;
